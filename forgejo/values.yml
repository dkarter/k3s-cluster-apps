# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/app-template-4.6.2/charts/other/app-template/values.schema.json
host: &host 'forgejo.k3s.pro'
http_port: &http_port 3000

controllers:
  main:
    strategy: Recreate
    replicas: 1

    containers:
      app:
        image:
          repository: codeberg.org/forgejo/forgejo
          tag: 15.0.0
          pullPolicy: IfNotPresent

        env:
          FORGEJO__server__DOMAIN: *host
          FORGEJO__server__ROOT_URL: 'https://forgejo.k3s.pro'
          FORGEJO__server__HTTP_PORT: *http_port
          FORGEJO__server__SSH_DOMAIN: *host
          FORGEJO__server__SSH_PORT: '2222'
          FORGEJO__server__SSH_LISTEN_PORT: '2222'
          FORGEJO__actions__ENABLED: 'true'
          FORGEJO__actions__DEFAULT_ACTIONS_URL: https://github.com
          FORGEJO__database__DB_TYPE: postgres
          FORGEJO__database__HOST: forgejo-db-rw.forgejo.svc.cluster.local:5432
          FORGEJO__database__NAME: app
          FORGEJO__database__USER:
            valueFrom:
              secretKeyRef:
                name: forgejo-db-app
                key: username
          FORGEJO__database__PASSWD:
            valueFrom:
              secretKeyRef:
                name: forgejo-db-app
                key: password

        envFrom:
          - secretRef:
              name: forgejo-env

        probes:
          liveness: &probes
            enabled: true
            custom: true
            spec:
              httpGet:
                path: /
                port: *http_port
              initialDelaySeconds: 30
              periodSeconds: 10
              timeoutSeconds: 5
              failureThreshold: 3
          readiness: *probes

  runner:
    strategy: Recreate
    replicas: 1

    pod:
      securityContext:
        fsGroup: 1000

    containers:
      runner:
        image:
          repository: data.forgejo.org/forgejo/runner
          tag: 12
          pullPolicy: IfNotPresent

        command:
          - sh
          - -ec
          - |
              rm -f /data/.runner
              mkdir -p /data/.cache/act
              printf '%s\n' \
                'host:' \
                '  workdir_parent: /data/.cache/act' \
                '' \
                'server:' \
                '  connections:' \
                '    forgejo:' \
                "      url: $FORGEJO_INSTANCE_URL" \
                "      uuid: $FORGEJO_RUNNER_UUID" \
                "      token: $FORGEJO_RUNNER_REGISTRATION_TOKEN" \
                '' \
                'runner:' \
                '  labels:' \
                '    - ubuntu-latest:docker://docker.io/library/node:24-bookworm' \
                '    - docker:docker://docker.io/library/node:24-bookworm' \
                > /tmp/runner-config.yml

              exec forgejo-runner daemon --config /tmp/runner-config.yml

        env:
          DOCKER_HOST: tcp://localhost:2375
          FORGEJO_INSTANCE_URL: https://forgejo.k3s.pro/

        envFrom:
          - secretRef:
              name: forgejo-runner-env

        securityContext:
          runAsGroup: 1000
          runAsUser: 1000

      docker-in-docker:
        image:
          repository: docker
          tag: 29-dind
          pullPolicy: IfNotPresent

        args:
          - dockerd
          - --host=tcp://0.0.0.0:2375
          - --tls=false

        env:
          DOCKER_TLS_CERTDIR: ''

        securityContext:
          privileged: true

persistence:
  data:
    enabled: true
    type: persistentVolumeClaim
    accessMode: ReadWriteOnce
    size: 10Gi
    advancedMounts:
      main:
        app:
          - path: /data

  runner-data:
    enabled: true
    type: persistentVolumeClaim
    accessMode: ReadWriteOnce
    size: 5Gi
    advancedMounts:
      runner:
        runner:
          - path: /data

  docker-graph-storage:
    enabled: true
    type: emptyDir
    advancedMounts:
      runner:
        docker-in-docker:
          - path: /var/lib/docker

service:
  app:
    controller: main
    ports:
      http:
        port: *http_port
ingress:
  main:
    enabled: true
    annotations:
      gethomepage.dev/enabled: 'true'
      gethomepage.dev/name: Forgejo
      gethomepage.dev/description: Self-hosted Git service
      gethomepage.dev/group: Dev
      gethomepage.dev/icon: forgejo.png
      kubernetes.io/ingress.class: 'traefik'
      cert-manager.io/cluster-issuer: 'letsencrypt'
    className: 'traefik'
    hosts:
      - host: *host
        paths:
          - path: /
            pathType: Prefix
            service:
              identifier: app
              port: http
    tls:
      - hosts:
          - *host
        secretName: forgejo-ingress-cert
