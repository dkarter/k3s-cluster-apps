apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: media
  name: webserver-media
  namespace: paperless

spec:
  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: 100Mi
